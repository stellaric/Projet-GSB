{% extends 'base.html.twig' %}

{% block title %}Comptable{% endblock %}

{% block body %}
    <h1>Comptable</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comptable.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ comptable.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ comptable.prenom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ comptable.adresse }}</td>
            </tr>
            <tr>
                <th>Cp</th>
                <td>{{ comptable.cp }}</td>
            </tr>
            <tr>
                <th>Login</th>
                <td>{{ comptable.login }}</td>
            </tr>
            <tr>
                <th>Mdp</th>
                <td>{{ comptable.mdp }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('comptable_index') }}">back to list</a>

    <a href="{{ path('comptable_edit', {'id': comptable.id}) }}">edit</a>

    {{ include('comptable/_delete_form.html.twig') }}
{% endblock %}
